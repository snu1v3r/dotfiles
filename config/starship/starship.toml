
palette = "snu1v3r" 

format = """\
[╭─](bg:terminalback fg:terminalfront)\
[ ](fg:prev_bg bg:prev_fg)\
[$os]()\
$sudo\
[](fg:terminalback bg:terminalfront)\
[](fg:terminalfront bg:pathback)\
$directory\
${custom.gitclean}\
${custom.gitdirty}\
$git_status\
[](fg:prev_bg bg:terminalback)\
$fill\
[${custom.interface}](fg:terminalback bg:pathback)\
$hostname\
[](bg:terminalback fg:prev_bg)\
[─╮ ](bg:terminalback fg:terminalfront)\
$line_break\
[╰─](bg:terminalback fg:terminalfront)\
$character\
"""

right_format = """\
[](bg:terminalback fg:statusfront)\
$status\
$cmd_duration\
$time\
${custom.vpn}\
[](bg:terminalback fg:prev_bg)\
[─╯](bg:terminalback fg:terminalfront)\
"""


[hostname]
ssh_only = false
disabled = false
format = "[](bg:prev_bg fg:hostback)[ 💻 $hostname]($style)"
style = "fg:terminalback bg:hostback"

[os]
disabled = false
style = "fg:textfront bg:terminalfront"

[os.symbols]
Windows = "󰍲"
Ubuntu = "󰕈"
SUSE = ""
Raspbian = "󰐿"
Mint = "󰣭"
Macos = "󰀵"
Manjaro = ""
Linux = "󰌽"
Gentoo = "󰣨"
Fedora = "󰣛"
Alpine = ""
Amazon = ""
Android = ""
Arch = "󰣇"
Artix = "󰣇"
EndeavourOS = ""
CentOS = ""
Debian = "󰣚"
Redhat = "󱄛"
RedHatEnterprise = "󱄛"
Pop = ""

[directory]
format = "[ ](fg:pathfront bg:pathback)([$read_only]($read_only_style))[$path]($style)"
read_only_style = "fg:errorfront bg:prev_bg"
truncation_length = 3
truncate_to_repo = false
truncation_symbol = "…/"
style = "fg:textfront bg:prev_bg"
#style = "fg:pathfront bg:pathback"

[git_branch]
#format = '[](bg:gitback fg:prev_bg)[ $symbol$branch(:$remote_branch) ]($style)'
format = '[](bg:gitback fg:prev_fg)[ $symbol$branch(:$remote_branch) ]($style)'
symbol = "  "
#style = "fg:terminalback bg:gitback"
disabled = true
style = "fg:textfront bg:prev_fg"

[git_status]
conflicted = '=${count} '
untracked = '?${count} '
modified = '!${count} '
ahead = '⇡${count} '
behind = '⇣${count} '
diverged = '⇕${count} '
stashed = '${count} '
deleted = '${count} '
staged = '+${count} '
renamed = '»${count} '
style = "fg:prev_fg bg:prev_bg"
format = '[${ahead_behind}${all_status}]($style)'
use_git_executable = false


[hg_branch]
format = "[ $symbol$branch ]($style)"
symbol = " "

[cmd_duration]
min_time = 5000
format = "[](fg:gitback bg:prev_bg )[ $duration  ]($style)"
#format = "[](bg:terminalback fg:terminalfront)[ $duration  ]($style)[](bg:gitback fg:terminalfront)"
style = "fg:textfront bg:gitback"

[character]
success_symbol = '[❯](successfront)'
error_symbol = '[✗](errorfront)'

[time]
disabled = false
time_format = "%T" # Hour:Minute Format
format = '[](bg:prev_bg fg:terminalfront )[ $time 󱑍 ](fg:textfront bg:terminalfront)'

[fill]
symbol = '─'
style = 'bg:terminalback fg:terminalfront'

[status]
symbol = '$int ✘ '
not_found_symbol = '$int  '
success_symbol = '✔ '
success_style = 'fg:successfront bg:statusfront'
failure_style = 'fg:errorfront bg:statusfront'
format = '[$symbol]($style)'
signal_symbol ='󱐋'
map_symbol = true
disabled = false

[sudo]
symbol = '󱐋'
style = 'fg:prev_fg bg:prev_bg'
format ='[ $symbol]($style)'
disabled = false

[custom.gitclean]
command = "git branch --show-current"
when = "git status | grep -q 'nothing to commit'"
format = "([](fg:prev_bg bg:successfront)[(   $output )]($style))"
style = 'fg:textfront bg:prev_bg'

[custom.gitdirty]
command = "git branch --show-current"
when = "! git status | grep -q 'nothing to commit'"
format = "([](fg:prev_bg bg:gitback)[   $output ]($style))"
style = 'fg:textfront bg:prev_bg'

[custom.interface]
command = "ip route | grep -m1 default | awk '{print $9 \" 󰩠 \" $5}'"
when = true
format = "([](bg:prev_bg fg:pathback)[$output ]($style))"
style = 'fg:textfront bg:pathback'

[custom.vpn]
command = "ip route | head -n1 | awk '{print $3 \" 󱠾 \" $5}'"
when = "ip route | head -n1 | grep -qE '(tun|tap)'"
format = "[](bg:prev_bg fg:vpnback)[$output ]($style)"
style = 'fg:textfront bg:vpnback'

[palettes.snu1v3r]
terminalback = '#1A1B26' 
textfront = '#1A1B26' 
terminalfront = '#707691'
pathback = '#7299E5' 
#pathback = '#3465a4'
pathfront = '#D1D4D4'
gitback = '#CFA466'
statusfront = '#31344A'
successfront = '#98C86B'
errorfront = '#E84D44'
vpnback='#F1C0E4'
hostback='#F4B186'
