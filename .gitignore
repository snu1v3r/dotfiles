# Directories added, because these plugins get installed from github repo's and are not modified

# These are ignored because automatically generated by the theme script
config/bat/themes/bat-theme.tmTheme
config/btop/themes/current.theme
config/nvim/lua/plugins/theme.lua


# These are ignored because cachefiles for neovim
config/nvim/lazy-lock.json
config/nvim/lazyvim.json

# These are ignored because it is an icon cache
local/share/applications/mimeinfo.cache

# These are ignored because they are machine specific overrides
config/hypr/overrides.conf
config/bat/themes/bat-theme.tmTheme
config/btop/themes/current.theme
config/nvim/lazy-lock.json
